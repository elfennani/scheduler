import React from "react";

type Props = {};

const DashboardHead = (props: Props) => {
    return (
        <>
            <title>Dashboard</title>
        </>
    );
};

export default DashboardHead;
